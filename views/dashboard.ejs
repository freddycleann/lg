<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vivify-X Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            color: #fff;
            background-image: url('goldground.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
        }

        nav {
            display: flex;
            justify-content: center;
            padding: 20px 0;
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(0, 0, 0, 0.7);
            box-shadow: 0 6px 10px rgba(0, 0, 0, 0.5);
            z-index: 10;
        }

        nav a {
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            margin: 0 20px;
            font-size: 20px;
            padding: 12px 25px;
            border-radius: 30px;
            font-family: 'Cinzel', serif;
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
        }

        nav a::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, #ffcc33, #ffd700, #ffcc33);
            z-index: 1;
            transition: left 0.3s ease;
        }

        nav a:hover::before {
            left: 0;
        }

        nav a span {
            position: relative;
            z-index: 2;
        }

        nav a:hover span {
            color: #000;
        }

        .container {
            text-align: center;
            padding: 100px 20px 60px;
            max-width: 800px;
            margin: auto;
        }

        h1 {
            font-family: 'Cinzel', serif;
            font-size: 36px;
            margin-bottom: 20px;
            color: transparent;
            background: linear-gradient(90deg, #c29d38, #e5c16d);
            -webkit-background-clip: text;
            background-clip: text;
        }

        p {
            font-size: 18px;
            margin-bottom: 40px;
            color: #fff;
            line-height: 1.6;
        }

        .button-frame {
            margin-top: 20px;
        }

        .button {
            display: inline-block;
            background: linear-gradient(90deg, #ffcc33, #ffd700, #ffcc33);
            color: #000;
            text-decoration: none;
            font-weight: bold;
            font-size: 18px;
            padding: 12px 25px;
            border-radius: 8px;
            transition: all 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
            border: 2px solid #b8860b;
            text-shadow: 0 0 8px rgba(255, 215, 0, 0.8);
        }

        .button:hover {
            background: linear-gradient(90deg, #ffd700, #ffcc33, #ffd700);
            color: #fff;
            transform: scale(1.05);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.8);
        }

        footer {
            text-align: center;
            padding: 20px;
            background: rgba(0, 0, 0, 0.8);
            position: fixed;
            bottom: 0;
            width: 100%;
            color: #d4af37;
            font-family: 'Cinzel', serif;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    <nav>
        <a href="/"><span>Home</span></a>
        <a href="/tools"><span>Tools</span></a>
        <a href="/about"><span>About</span></a>
        <a href="/contact"><span>Contact</span></a>
    </nav>

    <!-- Dashboard Content -->
    <div class="container">
        <h1>Welcome, <%= username ? username : "User" %>!</h1>
        <p>
            This is your personal dashboard, designed to keep you connected with Vivify-X's powerful features. 
            Here, you can explore tools, manage your projects, and track your progress—all in one place!
        </p>
        <div class="button-frame">
            <a href="/logout" class="button">Logout</a>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        &copy; 2024 Vivify-X. All rights reserved.
    </footer>
</body>
</html>