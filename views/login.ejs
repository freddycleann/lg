<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <style>
        body {
            background: url('https://i.gifer.com/origin/7b/7b6e4bfe04b3be28c9cf17ccefd3c82c.gif') center/cover fixed;
        }
        .glowing-text {
            animation: glow 1.5s infinite alternate;
        }
        @keyframes glow {
            0% { text-shadow: 0 0 10px #ff4500; }
            100% { text-shadow: 0 0 30px #ff8c00; }
        }
        .shake:hover {
            animation: shake 0.2s ease-in-out infinite;
        }
        @keyframes shake {
            0% { transform: translateX(0); }
            25% { transform: translateX(-3px); }
            50% { transform: translateX(3px); }
            75% { transform: translateX(-3px); }
            100% { transform: translateX(3px); }
        }
    </style>
</head>
<body class="h-screen flex justify-center items-center" style="background: url('https://i.gifer.com/VpZl.gif') center/cover fixed !important;">
    <div class="bg-gray-900 p-10 rounded-2xl shadow-2xl w-full max-w-lg transform transition-all duration-300 hover:scale-105">
        <h2 class="text-4xl font-extrabold text-center text-yellow-400 mb-8 glowing-text">🔥 Welcome Back 🔥</h2>

        <form action="/login" method="POST" class="space-y-6">
            <div>
                <label for="email" class="block text-sm font-semibold text-orange-300">Email</label>
                <input type="email" id="email" name="email" required
                    class="w-full px-6 py-3 mt-2 border-2 border-yellow-500 bg-gray-800 rounded-lg focus:ring-4 focus:ring-red-400 text-white placeholder-gray-400">
            </div>

            <div>
                <label for="password" class="block text-sm font-semibold text-orange-300">Password</label>
                <input type="password" id="password" name="password" required
                    class="w-full px-6 py-3 mt-2 border-2 border-yellow-500 bg-gray-800 rounded-lg focus:ring-4 focus:ring-red-400 text-white placeholder-gray-400">
            </div>

            <div class="flex justify-between items-center">
                <label class="flex items-center text-sm text-gray-400">
                    <input type="checkbox" class="mr-2 text-red-500"> Remember Me
                </label>
                <a href="#" class="text-sm text-yellow-400 hover:text-yellow-600 font-semibold glowing-text">Forgot Password?</a>
            </div>

            <button type="submit"
                class="w-full py-3 px-4 bg-gradient-to-r from-red-600 via-orange-500 to-yellow-400 text-white font-bold rounded-lg shake hover:opacity-90 focus:ring-4 focus:ring-red-600 transition-all duration-300">
                Log In 🔥
            </button>
        </form>

        <div class="mt-6 text-center">
            <p class="text-sm text-gray-400">Don't have an account yet? <a href="/register"
                    class="text-yellow-400 hover:text-yellow-600 font-semibold">Sign up here!</a></p>
        </div>
    </div>
</body>
</html>

